# 0.3.0 UX playtest

## Automated and developer checks

- Build validation checks all profile textures and all translated UX keys.
- The settings screen, key registrations, profile persistence, and visible-target restriction are covered by source and resource validation.
- The development client starts with `clear`, creates `distinctcraft-client.toml` with target feedback disabled, and stores the default `O`, `P`, and `H` bindings in Minecraft options.
- Developer smoke test: open the baseline test wall, aim at each three-block-high column, and compare outline visibility against dirt and vanilla stone.

## Target-player session

Pending. Test with the target player before closing 0.3.0:

1. Compare Subtle, Clear, and Monochrome on the baseline smoke-test wall.
2. Toggle the target outline with `H` and judge whether it is visible without being distracting.
3. Confirm that `O`, `P`, and `H` are memorable or remap them in Minecraft controls.
4. Record preferred profile, outline preference, and any requested tuning below.

### Feedback

- Preferred profile: pending
- Target outline: clearly communicates the selected block and is preferred over a second crosshair symbol
- Crosshair marker: rejected and removed; it looked like an unnecessary second `+`
- Key bindings: pending
- Tuning notes: pending
